events {}

http {
    upstream composition_service {
        least_conn;
        server composition1:16006;
        server composition2:16006;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://composition_service;
        }
    }
}